{
  "compilerOptions": {
    "noImplicitOverride": true,
    "useUnknownInCatchVariables": true
  },
  "tasks": {
    "lint": "deno fmt --check && deno lint && deno check --lock --remote main.ts",
    "test": "deno test --lock --check=all"
  }
}
